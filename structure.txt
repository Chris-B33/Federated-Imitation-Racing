┌───────────────────────────────────────────┐
│ Dolphin (Human)                           │
│ - Logs Telemetry (CSV per frame)          │
│ - Records Human Controller Inputs         │
└──────────────────┬────────────────────────┘
                   │
                   ▼
┌───────────────────────────────────────────┐
│ Federated Client (Docker)                 │
│ - Loads (telemetry + actions)             │
│ - Preprocesses data                       │
│ - Trains local imitation model            │
│ - Sends local model weights → Server      │
└──────────────────┬────────────────────────┘
                   │
                   ▼
┌───────────────────────────────────────────┐
│ Federated Server (Docker)                 │
│ - Aggregates updates (FedAvg)             │
│ - Builds updated Global Federated Model   │
│ - Distributes global model back to clients│
└──────────────────┬────────────────────────┘
                   │
          ┌────────┴─────────┐
          ▼                  ▼
┌──────────────────────────┐  ┌──────────────────────────┐
│ Centralized Model (Lab)  │  │ Federated Global Model   │
│ - Trained on pooled data │  │ - Aggregated client model│
│ - Used as comparison     │  │ - Represents avg style   │
│ - Evaluates differences  │  │ - Used for deployment    │
└───────────┬──────────────┘  └────────────┬─────────────┘
            │                              │
            ▼                              ▼
┌───────────────────────────────────────────┐
│ Evaluation / Analysis Layer               │
│ - Compare centralized vs federated models │
│ - Measure divergence & accuracy           │
│ - Tune aggregation / personalization      │
└──────────────────┬────────────────────────┘
                   │
                   ▼
┌───────────────────────────────────────────┐
│ Dolphin (AI)                              │
│ - Feeds telemetry live                    │
│ - Global or Centralized model outputs     │
│   controller actions                      │
│ - Actions injected via controller pipe    │
│ - Audience sees AI drive in real time     │
└───────────────────────────────────────────┘