version: "3.9"

services:
  server:
    build: ./server
    container_name: server
    ports:
      - "5000:5000"
    volumes:
      - model_storage:/app/models

  client:
    build: ./client
    container_name: client
    depends_on:
      - server

volumes:
  model_storage: